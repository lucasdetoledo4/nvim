-- Load configuration modules
require("config.options")    -- Options and settings
require("config.lazy")       -- Plugin manager (loads all plugins)
require("config.keymaps")    -- Key mappings
require("config.autocmds")   -- Auto commands
